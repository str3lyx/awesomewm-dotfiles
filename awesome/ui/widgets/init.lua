return {
   taglist = require("ui.widgets.taglist"),
   tasklist = require("ui.widgets.tasklist"),
   layoutbox = require("ui.widgets.layoutbox"),
   launcher = require("ui.widgets.launcher"),
   promp = require("ui.widgets.prompt")
}